services:
  - type: worker
    name: qasema-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: EXPIRE_DAYS
        value: "2"
